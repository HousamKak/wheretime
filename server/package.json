{
    "name": "where-time-server",
    "version": "1.0.0",
    "description": "Backend for Time Spent Visualization & Tracking Web App",
    "main": "server.js",
    "scripts": {
      "start": "node server.js",
      "dev": "nodemon server.js",
      "init-db": "node -e \"require('fs').existsSync('time_tracker.db') || require('fs').writeFileSync('time_tracker.db', '');\""
    },
    "dependencies": {
      "better-sqlite3": "^8.5.0",
      "cors": "^2.8.5",
      "dotenv": "^16.3.1",
      "express": "^4.18.2"
    },
    "devDependencies": {
      "nodemon": "^3.0.1"
    }
  }